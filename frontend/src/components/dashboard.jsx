/// dashboards for the DAO user to check proposals and vote on them
/// they can add a member directly from the requests
/// approve some requests to add the DAO user
/// token Gated only for DAO users
